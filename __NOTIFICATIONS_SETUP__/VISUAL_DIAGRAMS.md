# Visual Architecture Diagrams

## 📊 Centralized Notification System Architecture

### System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     Django Project Root                          │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │         __NOTIFICATIONS_SETUP__/  (CENTRAL HUB)           │  │
│  │                                                            │  │
│  │  ├── code/                                                │  │
│  │  │   ├── consumers.py         (WebSocket consumer)       │  │
│  │  │   ├── routing.py           (URL routing)              │  │
│  │  │   └── task_notifications.py (Helper functions)        │  │
│  │  │                                                         │  │
│  │  ├── settings/                                            │  │
│  │  │   ├── channels_config.py   (Settings reference)       │  │
│  │  │   └── asgi_config.py       (ASGI reference)           │  │
│  │  │                                                         │  │
│  │  ├── examples/                                            │  │
│  │  │   ├── javascript_integration.js                        │  │
│  │  │   └── react_integration.jsx                            │  │
│  │  │                                                         │  │
│  │  └── docs/                                                │  │
│  │      ├── README.md                                        │  │
│  │      ├── ARCHITECTURE.md                                  │  │
│  │      ├── MIGRATION_SUMMARY.md                             │  │
│  │      └── INSTALLATION_CHECKLIST.md                        │  │
│  └────────────────────────────────────────────────────────────┘  │
│                              ▲                                    │
│                              │ All imports come from here         │
│                              │                                    │
│  ┌───────────────────────────┴───────────────────────────────┐  │
│  │                    Project Modules                         │  │
│  │                                                             │  │
│  │  budget_transfer/                                          │  │
│  │  ├── asgi.py ──────────imports routing────────────┐       │  │
│  │  └── settings.py (Channels config)                │       │  │
│  │                                                     │       │  │
│  │  budget_management/                                │       │  │
│  │  └── tasks.py ──────imports task_notifications────┤       │  │
│  │                                                     │       │  │
│  │  oracle_fbdi_integration/utilities/                │       │  │
│  │  └── Upload_essjob_api.py ──imports helpers───────┘       │  │
│  │                                                             │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow Diagram

### Budget Transfer Submission → WebSocket Notifications

```
User Submits Transfer
       │
       ▼
┌─────────────────┐
│  Django View    │ Submit transfer, return 200 OK immediately
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Django Signal   │ Post-save signal triggered
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────────────┐
│  upload_journal_to_oracle.delay()            │ Queue Celery task
└────────┬────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────────┐
│                    Celery Worker                                │
│                                                                  │
│  1. Get user_id from transaction                               │
│  2. send_upload_started() ────────┐                            │
│  3. send_progress_notification()  │                            │
│  4. set_notification_user()       │                            │
│  5. run Oracle workflow           │ All imported from          │
│  6. send_workflow_notification()  │ __NOTIFICATIONS_SETUP__    │
│  7. send_upload_completed()  ─────┘                            │
│                                                                  │
└────────────────────────┬───────────────────────────────────────┘
                         │
                         ▼
                  ┌─────────────┐
                  │    Redis    │ Channel layer pub/sub
                  └──────┬──────┘
                         │
                         ▼
        ┌────────────────────────────────┐
        │  Django Channels (Daphne)      │
        │                                 │
        │  NotificationConsumer           │
        │  (from __NOTIFICATIONS_SETUP__) │
        └────────┬───────────────────────┘
                 │
                 ▼
        ┌────────────────┐
        │   WebSocket    │ Real-time notification
        │   Connection   │
        └────────┬───────┘
                 │
                 ▼
        ┌────────────────┐
        │  Browser/UI    │ Display progress
        └────────────────┘
```

---

## 📁 Import Dependency Graph

```
__NOTIFICATIONS_SETUP__/code/
├── consumers.py
├── routing.py
│   └── imports consumers.py
└── task_notifications.py
    └── imports (channels.layers, asgiref.sync, logging)

                    ▲
                    │
        ┌───────────┼───────────┐
        │           │           │
        │           │           │
   
budget_transfer/    budget_management/    oracle_fbdi_integration/
    asgi.py              tasks.py             Upload_essjob_api.py
       │                    │                        │
       │                    │                        │
    imports             imports                  imports
    routing          task_notifications      task_notifications
```

---

## 🌐 WebSocket Connection Flow

```
Browser                    Django (Daphne)                Redis
  │                              │                          │
  │  ws://localhost:8000/        │                          │
  │  ws/notifications/           │                          │
  ├─────────────────────────────▶│                          │
  │                              │                          │
  │         WebSocket            │  1. Check auth           │
  │         Handshake            │  2. Create group         │
  │◀─────────────────────────────┤     "notifications_123"  │
  │                              │                          │
  │  connection_established      │  3. Join group           │
  │◀─────────────────────────────┤─────────────────────────▶│
  │                              │                          │
  │                              │                          │
  │                      Celery Task Sends Notification     │
  │                              │◀─────────────────────────┤
  │                              │  group_send()            │
  │                              │                          │
  │  Notification Message        │                          │
  │◀─────────────────────────────┤                          │
  │  (oracle_upload_progress)    │                          │
  │                              │                          │
```

---

## 📊 Celery Task Execution Flow

```
upload_journal_to_oracle(transaction_id=602, entry_type="submit")
│
├─ Get transaction & user_id
│
├─ send_upload_started(user_id, transaction_id)
│  └─▶ WebSocket: "Upload started"
│
├─ send_progress_notification(...step 1/5...)
│  └─▶ WebSocket: "Preparing Data (1/5)"
│
├─ set_notification_user(user_id)
│  └─▶ Sets global variable for workflow
│
├─ create_and_upload_journal()
│  │
│  ├─ send_workflow_notification(...step 2/5...)
│  │  └─▶ WebSocket: "Upload to UCM (2/5)"
│  │
│  ├─ send_workflow_notification(...step 3/5...)
│  │  └─▶ WebSocket: "Interface Loader (3/5)"
│  │
│  ├─ send_workflow_notification(...step 4/5...)
│  │  └─▶ WebSocket: "Journal Import (4/5)"
│  │
│  └─ send_workflow_notification(...step 5/5...)
│     └─▶ WebSocket: "AutoPost (5/5)"
│
└─ send_upload_completed(user_id, transaction_id, result_path)
   └─▶ WebSocket: "Upload completed successfully"
```

---

## 🔐 Authentication & Security Flow

```
Browser                WebSocket               Django
  │                       │                      │
  │  HTTP Login           │                      │
  ├──────────────────────────────────────────────▶│
  │                       │                      │
  │  Session Cookie       │                      │
  │◀──────────────────────────────────────────────┤
  │                       │                      │
  │  WebSocket Connect    │                      │
  │  (with session)       │                      │
  ├──────────────────────▶│                      │
  │                       │  Check session       │
  │                       ├─────────────────────▶│
  │                       │                      │
  │                       │  User authenticated  │
  │                       │◀─────────────────────┤
  │                       │                      │
  │  Connection Success   │  Create group:       │
  │◀──────────────────────┤  notifications_{id}  │
  │                       │                      │
```

---

## 📦 Component Interaction Diagram

```
┌────────────────────────────────────────────────────────────┐
│                     Frontend (Browser)                      │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   UI Form    │  │  WebSocket   │  │  Progress    │     │
│  │              │  │  Connection  │  │     Bar      │     │
│  └──────┬───────┘  └──────┬───────┘  └──────▲───────┘     │
│         │                 │                  │              │
└─────────┼─────────────────┼──────────────────┼──────────────┘
          │                 │                  │
          ▼                 ▼                  ▲
┌────────────────────────────────────────────────────────────┐
│                    Django Backend                           │
│                                                              │
│  ┌────────────┐   ┌──────────────────┐   ┌──────────────┐ │
│  │   Views    │   │  NotificationCon │   │  WebSocket   │ │
│  │            │   │     sumer        │   │   Events     │ │
│  │            │   │  (__NOTIF...)    │   │              │ │
│  └─────┬──────┘   └────────▲─────────┘   └──────▲───────┘ │
│        │                   │                     │          │
│        ▼                   │                     │          │
│  ┌────────────┐            │              ┌──────┴───────┐ │
│  │  Signals   │            └──────────────┤    Redis     │ │
│  └─────┬──────┘                           │ Channel Layer│ │
│        │                                  └──────▲───────┘ │
│        ▼                                          │          │
│  ┌────────────────────────┐                      │          │
│  │   Celery Task          │                      │          │
│  │  (__NOTIF...helpers)   │──────────────────────┘          │
│  └────────────────────────┘                                 │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 🎯 Data Flow: Notification Sending

```
Step 1: Celery Task Calls Helper
┌────────────────────────────────┐
│  send_upload_progress(         │
│    user_id=123,                │
│    transaction_id=602,         │
│    step="Upload to UCM",       │
│    step_number=2,              │
│    total_steps=5               │
│  )                             │
└────────┬───────────────────────┘
         │
         ▼
Step 2: Helper Formats Message
┌────────────────────────────────┐
│  {                             │
│    'type': 'oracle_upload_     │
│             progress',         │
│    'transaction_id': 602,      │
│    'step': 'Upload to UCM',    │
│    'step_number': 2,           │
│    'total_steps': 5,           │
│    'timestamp': '2025-11-18...'│
│  }                             │
└────────┬───────────────────────┘
         │
         ▼
Step 3: Send to Redis Channel
┌────────────────────────────────┐
│  channel_layer.group_send(     │
│    'notifications_123',        │
│    message_dict                │
│  )                             │
└────────┬───────────────────────┘
         │
         ▼
Step 4: Redis Publishes to Group
┌────────────────────────────────┐
│  PUBLISH notifications_123     │
│  {message_dict}                │
└────────┬───────────────────────┘
         │
         ▼
Step 5: Consumer Receives
┌────────────────────────────────┐
│  NotificationConsumer.         │
│    oracle_upload_progress()    │
└────────┬───────────────────────┘
         │
         ▼
Step 6: Send to WebSocket
┌────────────────────────────────┐
│  self.send(text_data=...)      │
└────────┬───────────────────────┘
         │
         ▼
Step 7: Browser Receives
┌────────────────────────────────┐
│  ws.onmessage = (event) => {   │
│    const data = JSON.parse(..) │
│    updateProgressBar(data)     │
│  }                             │
└────────────────────────────────┘
```

---

**Created:** November 18, 2025  
**Version:** 2.0 (Centralized Architecture)
